# Local API. For a hosted build, set this to your live API with no trailing slash:
# VITE_API_URL=https://your-api.example.com
VITE_API_URL=http://localhost:5000
# Vite dev server only. Production CORS is set on the API (CORS_ORIGIN).
VITE_CORS_ORIGIN=http://localhost:5173
